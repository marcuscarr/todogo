version: '3.1'

services:

  db:
    image: postgres
    restart: always
    volumes:
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    environment:
      POSTGRES_PASSWORD: mysecretpassword

  app:
    image: todogo
    restart: always
    ports:
      - 8080:8080

  adminer:
    image: adminer
    restart: always
    ports:
      - 8180:8080